import CoreGraphics

/// Shared layout constants for article and thread reader/detail screens.
/// Use `hPadding` for horizontal content insets so all reader views stay visually consistent.
enum ReaderLayout {
    static let hPadding: CGFloat = 20
}
